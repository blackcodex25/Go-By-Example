package main

import (
	"fmt"
	"time"
)

func main() {
	p := fmt.Println  // ตัวแปร p เป็นการย่อคำสั่ง fmt.Println เพื่อให้เรียกใช้ได้สะดวก
	now := time.Now() // ตัวแปร now ให้เก็บคำนวณเวลาปัจจุบัน
	fmt.Println(now)  // แสดงเวลาปัจจุบัน

	p(now.Unix())      // แสดงเวลาปัจจุบันเป็น Unix time นับเป็นวินาที
	p(now.UnixMilli()) // แสดงเวลาปัจจุบันเป็น Unix time นับเป็นมิลลิวินาที
	p(now.UnixNano())  // แสดงเวลาปัจจุบันเป็น Unix time นับเป็นวินาที

	p(time.Unix(now.Unix(), 0))     // แปลง Unix time ที่เป็นวินาที (now.Unix()) กลับมาเป็น time.Time
	p(time.Unix(0, now.UnixNano())) // แปลง Unix time ที่เป็นวินาที (now.UnixNano()) กลับมาเป็น time.Time

}

/* อธิบายโค้ดที
p := fmt.Println
สร้างตัวแปร p เพื่อทำให้คำสั่ง fmt.Println สั้นลง
เวลาจะพิมพ์ผลลัพธ์ออกมาเราสามารถใช้ p(...) แทน fmt.Println(...)

now := time.Now()
ดึงเวลา ปัจจุบัน มาเก็บไว้ในตัวแปร now โดยใช้ฟังก์ชัน time.Now()
ซึ่งคืนค่าเป็นประเภท time.Time

fmt.Println(now)
แสดงผลลัพธ์ของเวลาปัจจุบันออกมาบนหน้าจอ เช่น
2024-09-11 13:45:30.123456789 +0000 UTC

p(now.Unix())
แปลงเวลาปัจจุบัน (now) ให้เป็น Unix time ในหน่วย วินาที
โดยใช้ฟังก์ชัน now.Unix() เช่น 1726094400
(นี่คือจำนวนวินาทีที่ผ่านไปตั้งแต่ 1 มกราคม 1970)

p(now.UnixMilli())
แปลงเวลาปัจจุบันให้เป็น Unix time ในหน่วย มิลลิวินาที
(1 มิลลิวินาที = 1/1000 ของวินาที) โดยใช้ now.UnixMilli()

p(now.UnixNano())
แปลงเวลาปัจจุบันให้เป็น Unix time ในหน่วย นาโนวินาที
(1 นาโนวินาที = 1/1,000,000,000 ของวินาที)
โดยใช้ now.UnixNano()

p(time.Unix(now.Unix(), 0))
แปลง Unix time ที่เป็นวินาที (now.Unix()) กลับมาเป็น
time.Time โดยใช้ฟังก์ชัน time.Unix()
time.Unix(seconds, nanoseconds) รับสองพารามิเตอร์คือ
จำนวนวินาทีและจำนวนนาโนวินาที (ที่นี่เราใส่ 0 เพราะสนใจแค่หน่วยวินาที)

p(time.Unix(0, now.UnixNano()))
แปลง Unix time ที่เป็นนาโนวินาที (now.UnixNano()) กลับมาเป็น
time.Time โดยใส่ค่า 0 ในพารามิเตอร์วินาที และใส่ค่า now.UnixNano()
เป็นค่าของนาโนวินาทีแทน
*/
/* สรุป
Unix time คือจำนวนวินาทีที่ผ่านไปนับตั้งแต่วันที่ 1 มกราคม 1970 เวลา 00:00:00 UTC
โค้ดนี้แสดงให้เห็นการแปลงเวลาปัจจุบันไปเป็น Unix time ใน หน่วยวินาที, มิลลิวินาที, และนาโนวินาที
นอกจากนี้ยังแสดงวิธีแปลงค่ากลับจาก Unix time กลับไปเป็น time.Time
*/
